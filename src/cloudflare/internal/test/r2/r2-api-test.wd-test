using Workerd = import "/workerd/workerd.capnp";

const unitTests :Workerd.Config = (
  services = [
    ( name = "r2-api-test",
      worker = (
        modules = [
          ( name = "worker", esModule = embed "r2-api-test.js" )
        ],
        compatibilityDate = "2023-07-24",
        compatibilityFlags = ["nodejs_compat", "service_binding_extra_handlers"],
				bindings = [
					(
						name = "BUCKET",
						wrapped = (
							moduleName = "cloudflare-internal:r2-api",
							innerBindings = [(
								name = "fetcher",
								service = "r2-upstream-mock"
							)]
						)
					)
				]
      )
    ),
		( name = "r2-upstream-mock",
      worker = (
        compatibilityDate = "2023-07-24",
        compatibilityFlags = ["nodejs_compat", "service_binding_extra_handlers"],
        modules = [
          ( name = "worker", esModule = embed "r2-upstream-mock.js" )
        ],
      )
    )
  ],
);
